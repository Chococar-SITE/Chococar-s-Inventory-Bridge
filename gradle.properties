# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx4G

# Gradle 9.0 優化配置
org.gradle.configuration-cache=true
org.gradle.configuration-cache.parallel=true
org.gradle.build-cache=true
org.gradle.parallel=true

# Java Home Configuration
org.gradle.java.home=C:\\Program Files\\Java\\jdk-21

# Fabric Properties (will be overridden by CI)
minecraft_version=1.21.4
yarn_mappings=1.21.4+build.8
loader_version=0.16.9

# Mod Properties
mod_version=1.0.0-SNAPSHOT
maven_group=site.chococar
archives_base_name=chococars-inventory-bridge

# Dependencies (will be overridden by CI)
fabric_version=0.119.0+1.21.4
paper_version=1.21.4-R0.1-SNAPSHOT
data_version=4082

# CI/CD Properties
ci_build=false

# Dependency versions
junit_version=5.10.1
mockito_version=5.8.0
h2_version=2.2.220